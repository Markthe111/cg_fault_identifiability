# Demo

Run the synthetic demo from the repository root:

```bash
python scripts/run_synthetic_demo.py
```

The demo uses artificial local coordinates and does not contain real mine data.
It creates two synthetic fault domains, assigns synthetic ore points to the
nearest fault surface, and writes small tables/figures under the script output
folder.
